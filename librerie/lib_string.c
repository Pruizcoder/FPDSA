#include <stdio.h>
#include <stdlib.h>
#include <lib_string.h>
#define MAX 32

int cerca_spazio(char *v, int start, int stop)
{
   for (int i = start; i < stop; i++)
   {
      if (v[i] == ' ')
      {
         
      }
      
   }
   
}